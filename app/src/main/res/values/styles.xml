<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.MyApp" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Темная тема для всего приложения, подключается в манифесте. Наследование от темной статичной темы, вне зависимости выбора пользователя -->
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="Theme.myDarkTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darkThemeBackground</item>
        <!-- Customize your theme here. -->

        <!--Нижняя панель навигации с кнопками-->
        <item name="android:navigationBarColor">@color/darkThemeBackground</item>

        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>